# Figures

This directory is reserved for figures generated from an authorised copy of the source data, such as:

- rolling factor correlations;
- realised Sharpe ratio across Ridge penalties; and
- expected versus realised portfolio returns using the same alpha and weights.

No figures are included until the cleaned notebook can be rerun against an authorised dataset.

